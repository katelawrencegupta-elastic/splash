# Splash load-test harness
#
# Install (from this directory):
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Stack with metrics ports on loopback:
#   cd .. && docker compose -f docker-compose.yml -f docker-compose.loadtest.yml up --build -d
#
# Run a scenario:
#   python -m loadtest run --scenario S1 --eps 5000 --duration 120
#   python -m loadtest run --scenario S3 --eps 10000 --duration 300
#
# Results land in ./results/<run-id>/

pyyaml>=6.0
httpx>=0.27
