http.host: "0.0.0.0"
xpack.monitoring.enabled: false
# pipeline.workers omitted: Logstash defaults to available CPU cores ("auto").
# Explicit "auto" is invalid (must be Integer).
pipeline.batch.size: 500
